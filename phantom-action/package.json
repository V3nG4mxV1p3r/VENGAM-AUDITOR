{
  "name": "vengam-gamesec-action",
  "version": "7.0.0",
  "description": "VENGAM Auditor — GitHub Action",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build src/index.ts -o dist --quiet",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@actions/artifact": "^2.1.0",
    "@actions/core":     "^1.10.1",
    "@actions/exec":     "^1.1.1"
  },
  "devDependencies": {
    "@types/node":   "^20.0.0",
    "@vercel/ncc":   "^0.38.0",
    "typescript":    "^5.5.0"
  }
}
